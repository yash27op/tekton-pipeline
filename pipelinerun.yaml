apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  name: run-script-helper-pod-pipelinerun
spec:
  pipelineRef:
    name: run-script-pipeline
  workspaces:
    - name: scripts
      persistentVolumeClaim:
        claimName: scripts-pvc
